<div class="ui container fix">

<div style="margin-top: 20%;">
 	<% @users.each do |user| %>
			<% if user != current_user %>
			<h1 style="text-align: center;"> <%= link_to "#{user.full_name}", user_path(user), :class => "black"  %>
			</h1>
			<% end %>

	<% end %>
</div>

</div>
